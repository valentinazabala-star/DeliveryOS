node_modules/
build/
dist/
coverage/
.DS_Store
*.log

# Secrets — never commit
.env
.env.local
.env.*.local

# Large auto-generated cache — regenerates on startup (~80 min first boot)
data/cache-snapshot.json

# Keep everything else in data/ (UUIDs, history, statuses, overrides)
